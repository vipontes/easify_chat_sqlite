name: easify_chat_sqlite
description: A new Flutter package.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.17.0
  moor_flutter: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  moor_generator: ^2.2.0
  build_runner:

flutter:
